[pytest]
markers =
    setup: prepare the execution of all tests and execute all events
    regression: all assertions related to the cases
    placeholder: only used for the data placement holder
log_cli = 1
log_cli_level = INFO
