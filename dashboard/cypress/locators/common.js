export const localeContent = {
	ACCESS_DENIED_PAGE_HEADING: "Access Denied",
};

export const appendCurrencyLabel = (label) => `${label} ($)`;
export const appendPercentageLabel = (label) => `${label} %`;
